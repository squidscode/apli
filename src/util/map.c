#ifndef MAP_C
#define MAP_C

#include "list.c"

// NOTE: Value_type must have a list predefined in order for
// this to work.
#define define_map(key_type, value_type) \
    



define_map(int,int);

#endif